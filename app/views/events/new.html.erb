<h1>New event</h1>

<%= simple_form_for @event do |f| %>


<div class="form-inputs">
	<%= f.input :timeline_pic %>
	<%= f.input :happened_at %>
	<%= f.input :content %>
</div>

<div class="form-actions">
	<%= f.button :submit %>
</div>

<% end %>